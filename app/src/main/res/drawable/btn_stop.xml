<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:dither="true"
    android:shape="oval">
    <gradient
        android:centerColor="@color/colorStopMain"
        android:endColor="@color/colorStrokeMain"
        android:gradientRadius="70dp"
        android:startColor="@color/colorStopMain"
        android:type="radial" />
    <size
        android:width="100dp"
        android:height="100dp" />
    <stroke
        android:width="2dp"
        android:color="@color/colorStrokeMain" />
</shape>